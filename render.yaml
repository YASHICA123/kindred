# This configuration is deprecated
# The school-cms (Strapi) has been replaced with Supabase
# The main Next.js app is now deployed on Vercel

# For Vercel deployment, see DEPLOYMENT_GUIDE.md
# For old CMS reference (removed):
# services:
#   - type: web
#     name: school-cms
#     runtime: node
#     rootDir: school-cms
#     buildCommand: npm ci; npm run build
#     startCommand: npm run start
